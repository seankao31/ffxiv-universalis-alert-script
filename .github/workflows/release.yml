name: Release to update branch

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build userscript
        run: node build.js

      - name: Extract meta file
        run: sed -n '/==UserScript==/,/==\/UserScript==/p' universalis-alert.user.js > universalis-alert.meta.js

      - name: Deploy to release branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create an orphan branch with only the two release files
          git checkout --orphan release-tmp
          git rm -rf .
          git add universalis-alert.user.js universalis-alert.meta.js
          git commit -m "Release ${GITHUB_REF_NAME:-manual}"

          # Force-push to the release branch
          git push origin release-tmp:release --force
